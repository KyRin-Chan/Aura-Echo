name: Build RTX 5080 CUDA Optimized Executable

on:
  push:
    branches: [ master-custom ]
  pull_request:
    branches: [ master-custom ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build Type (auto, lightweight, full, full-patch)'
        required: true
        default: 'auto'
        type: choice
        options:
        - auto
        - lightweight
        - full
        - full-patch

env:
  BACKEND: cuda
  BUILD_NAME: rtx5080-optimized
  PYTHONWARNINGS: ignore
  PYINSTALLER_CACHE_DIR: ${{ github.workspace }}/.pyinstaller_cache

jobs:
  build-win-cuda-5080:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Determine Build Type
      id: determine_build
      shell: bash
      run: |
        BUILD_TYPE="${{ inputs.build_type || 'auto' }}"
        
        if [ "$BUILD_TYPE" = "auto" ]; then
          BUILD_TYPE="lightweight"
          
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            CHANGED_FILES=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.event.pull_request.head.sha }})
          elif [ "${{ github.event_name }}" = "push" ] && [ -n "${{ github.event.before }}" ] && [ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]; then
            CHANGED_FILES=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }} 2>/dev/null || echo "ALL")
          else
            CHANGED_FILES="ALL"
          fi
          
          echo "Changed files:"
          echo "$CHANGED_FILES"
          
          if echo "$CHANGED_FILES" | grep -qE "requirements.*\.txt|MMVCServerSIO\.spec|\.github/workflows/"; then
            echo "Build configuration or dependency file changed. Selecting FULL-PATCH build."
            BUILD_TYPE="full-patch"
          elif [ "$CHANGED_FILES" = "ALL" ]; then
            echo "Could not fetch diff. Selecting FULL-PATCH build as fallback."
            BUILD_TYPE="full-patch"
          else
            echo "Only logic files changed. Selecting LIGHTWEIGHT build."
          fi
        fi
        
        echo "Final Build Type: $BUILD_TYPE"
        echo "build_type=${BUILD_TYPE}" >> $GITHUB_OUTPUT

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: |
          client/lib/package-lock.json
          client/modern-gui/package-lock.json

    - name: Install and Build Client Library
      working-directory: ./client/lib
      run: |
        npm ci --legacy-peer-deps
        npm run build:prod

    - name: Install and Build Modern GUI
      working-directory: ./client/modern-gui
      run: |
        npm ci --legacy-peer-deps
        xcopy "..\lib\dist\*" "node_modules\@dannadori\voice-changer-client-js\dist\" /S /E /Y /I
        npm run build:prod

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Cache Virtual Environment
      uses: actions/cache@v4
      id: cache-venv
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ steps.determine_build.outputs.build_type }}-${{ hashFiles('server/requirements-common.txt', 'server/requirements-cuda-5080.txt') }}

    - name: Cache PyInstaller Build Cache
      uses: actions/cache@v4
      with:
        path: server/build
        key: pyinstaller-build-${{ runner.os }}-${{ steps.determine_build.outputs.build_type }}-${{ github.sha }}
        restore-keys: |
          pyinstaller-build-${{ runner.os }}-${{ steps.determine_build.outputs.build_type }}-

    - name: Setup Virtual Environment
      if: steps.cache-venv.outputs.cache-hit != 'true'
      run: |
        python -m venv .venv
        .venv\Scripts\python -m pip install --upgrade pip wheel setuptools pyinstaller

    - name: Install Common Dependencies
      working-directory: ./server
      run: |
        ..\.venv\Scripts\python -m pip install -r requirements-common.txt

    - name: Install CUDA Dependencies (Full Build Only)
      if: steps.determine_build.outputs.build_type != 'lightweight'
      working-directory: ./server
      run: |
        ..\.venv\Scripts\python -m pip install -r requirements-cuda-5080.txt

    - name: Cache PyInstaller Directory
      uses: actions/cache@v4
      with:
        path: .pyinstaller_cache
        key: pyinstaller-cache-${{ runner.os }}-${{ hashFiles('server/requirements-common.txt', 'server/requirements-cuda-5080.txt') }}

    - name: Create PyInstaller Cache Directory
      run: mkdir -p .pyinstaller_cache
      shell: bash

    - name: Building executable (RTX 5080 Spec)
      working-directory: ./server
      run: |
        ..\.venv\Scripts\python -m PyInstaller --clean -y --dist ./dist MMVCServerSIO.spec
      env:
        LIGHTWEIGHT_BUILD: ${{ steps.determine_build.outputs.build_type == 'lightweight' }}

    - name: Copy utils
      run: cp ./server/{force_gpu_clocks.bat,reset_gpu_clocks.bat} ./server/dist/
      shell: bash

    - name: Pack artifact
      shell: bash
      run: |
        mkdir ./server/artifacts
        ARCHIVE_NAME="voice-changer-windows-amd64-cuda-5080"
        EXTRA_7Z_ARGS=""
        VOLUME_ARG="-v2000m"
        if [ "${{ steps.determine_build.outputs.build_type }}" = "lightweight" ]; then
          ARCHIVE_NAME="${ARCHIVE_NAME}-lightweight"
        elif [ "${{ steps.determine_build.outputs.build_type }}" = "full-patch" ]; then
          ARCHIVE_NAME="${ARCHIVE_NAME}-patch"
          EXTRA_7Z_ARGS="-xr!torch -xr!torchaudio -xr!tensorrt -xr!onnxruntime -xr!onnx -xr!onnxscript -xr!onnxslim -xr!torchcrepe -xr!torchfcpe -xr!scipy -xr!faiss -xr!cublas*.dll -xr!cudnn*.dll -xr!cufft*.dll -xr!curand*.dll -xr!cusparse*.dll -xr!cusolver*.dll -xr!npp*.dll -xr!nvrtc*.dll -xr!libonnxruntime*.dll -xr!nvinfer*.dll -xr!tensorrt*.dll -xr!libnv*.dll"
          VOLUME_ARG=""
        fi
        echo "ARCHIVE_NAME=$ARCHIVE_NAME" >> $GITHUB_ENV
        7z a $EXTRA_7Z_ARGS $VOLUME_ARG ./server/artifacts/${ARCHIVE_NAME}.7z ./server/dist/* > /dev/null 2>&1

    - name: Rename single asset
      shell: bash
      run: |
        ls -al ./server/artifacts/
        if [ -f "./server/artifacts/${{ env.ARCHIVE_NAME }}.7z.001" ]; then
          mv ./server/artifacts/${{ env.ARCHIVE_NAME }}.7z.001 ./server/artifacts/${{ env.ARCHIVE_NAME }}.7z
        fi

    - name: Uploading artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.ARCHIVE_NAME }}
        path: ./server/artifacts
